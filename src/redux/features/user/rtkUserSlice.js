import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const rtkUserSlice = createSlice({
    name: "rtkUsers",
    initialState,
    reducers: {},
});
export const {} = rtkUserSlice.actions;
export default rtkUserSlice.reducer;
