import ProtectedClientPage from "./page";

export const metadata = {
    title: "Protected (Client)",
    description: "Protected page with client side rendering!",
};

export default ProtectedClientPage;
